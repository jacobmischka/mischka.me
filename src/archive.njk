---
layout: page
title: Archive
---
<p>
    An imported archive of posts from my old site.
</p>

<ul class="blog-list">
    {%- for post in collections.archived | reverse -%}
	<li>
	    <a href="{{ post.url }}">{{ post.data.title }}</a>
	    <time>{% formatDate post.date %}</time>

	    {% if post.data.lead %}
		— {{ post.data.lead }}
	    {% elif post.templateContent %}
		— {{ post.templateContent | striptags | truncate(50) }}
	    {% endif %}
	</li>
    {%- endfor -%}
</ul>
