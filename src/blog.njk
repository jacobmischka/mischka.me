---
layout: page
title: Blog
---
<ul class="blog-list">
    {%- for post in collections.post | reverse -%}
	<li>
	    <a href="{{ post.url }}">{{ post.data.title }}</a>
	    <time>{% formatDate post.date %}</time>

	    {% if post.data.lead %}
		— {{ post.data.lead }}
	    {% elif post.templateContent %}
		— {{ post.templateContent | striptags | truncate(50) }}
	    {% endif %}
	</li>
    {%- endfor -%}
</ul>

<div>
  <a href="/archive">View an archive of old posts</a>
</div>
